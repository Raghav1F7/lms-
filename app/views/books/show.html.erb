<!-- Middle Column -->
<div class="w-96 flex-shrink-0 border-r border-gray-200 bg-white z-10 flex flex-col">
  <%= turbo_frame_tag "list_frame" do %>
    <%= render "list", books: @books, selected_id: @selected_book.id %>
  <% end %>
</div>

<!-- Right Column -->
<div class="flex-1 flex flex-col min-w-0 bg-white">
  <%= turbo_frame_tag "detail_frame" do %>
    <%= render "detail", book: @selected_book, accessions: @accessions %>
  <% end %>
</div>
